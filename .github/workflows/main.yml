on:
  push:
    branches:
      - two




jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v3
      
      - name: "login to docker"
        run: docker login --username ameranegm20 --password dckr_pat_XO2ml5a_2f--tpzSZockczRBZNg
